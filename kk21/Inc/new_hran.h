#ifndef _NEW_HRAN_H
#define _NEW_HRAN_H

/*---------------------------------------------------- NEW HRAN
 * ------------------------------------------------------------*/
enum ErrorCode revolvCalibration(void);

enum ErrorCode revolvZOpen(void);

enum ErrorCode revolvZClose(void);

enum ErrorCode revolvZStp(int a);

enum ErrorCode revolvCheckCartridge(void);

#endif  //_NEW_HRAN_H
